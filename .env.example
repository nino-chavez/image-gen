# Generated from 1Password vault "Developer Secrets".
# Materialize values:  op inject -i .env.example -o .env
# Run inline:          op run --env-file=.env.example -- <cmd>
# Source file:         .env
#
# Only the OpenRouter key is read by this package (src/providers/openrouter.js).
# Non-secret script settings — IMAGE_GEN_REF_DIR, LPO_MASCOT_ANCHOR, FD_MODEL,
# FD_NOREF — are documented in the README, not materialized here.

OPENROUTER_API_KEY=op://Developer Secrets/OpenRouter image-gen/credential
